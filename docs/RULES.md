# RULES · Правила игры в 1000 на кубиках

Справка по самой игре и сопоставление каждого пункта с настройками приложения. Правила народные — в разных компаниях отличаются, поэтому всё ключевое вынесено в переключатели (см. [SPEC.md §4](SPEC.md#4-конфигурация-правил)).

---

## Что нужно

Пять костей, от 2 игроков. Цель — первым набрать **ровно 1000**. Очерёдность определяют броском одного кубика: у кого больше, тот первый.

## Подсчёт очков

Зачётные грани сами по себе — только **1** и **5**. Остальные приносят очки только в тройках (по номиналу).

| Комбинация | classic | fast |
|---|---|---|
| Одна единица | 10 | 100 |
| Одна пятёрка | 5 | 50 |
| Три двойки / тройки / … | 20 / 30 / … | ×100 |
| Три единицы | 100 | 1000 |
| Четыре одинаковых | удваивает тройку | удваивает тройку |
| Пять единиц | сразу победа | сразу победа |

> Приложение не считает комбинации само — игроки вписывают итог кона руками. Шкала (`scale`) влияет на быстрые кнопки ввода и тексты этой таблицы.

## Ход

Бросаешь все 5 костей. Откладываешь зачётные, остальные перебрасываешь, добирая очки. Хочешь — записываешь сумму кона; нет ни одной зачётной кости — **болт**, очки кона сгорают.

→ Настройка `samosval` добавляет вычет за каждый болт.

## Самосвал (555)

Счёт встал ровно на **555** — весь набранный счёт сбрасывается в ноль. Не кости, а именно сумма очков.

→ Настройка `dump555` (выкл / 1 / 2 / 3 / всегда). Число — сколько раз самосвал срабатывает за партию: после исчерпания лимита 555 уже не сбрасывает. «всегда» — каждый раз, «выкл» — правило отключено. Считается автоматически по сумме; кон, на котором счёт стал 555, помечается в таблице «самосвал».

## Вход в игру

Чтобы открыть счёт, нужно набрать **≥ порога** (обычно 120) за один кон. Пока не зашёл — очки не пишутся и в болты не идут.

→ Настройки `entry` (порог) и `entryMode`:

- **1 заход** (`once`) — зашёл один раз и в игре до конца, хоть в минус уходи.
- **Каждый раз** (`each`) — любой кон меньше порога обнуляет счёт в ноль, заходишь заново. Даёт долгую, нервную партию.

## Три болта подряд

Три болта (кона в ноль) кряду — штраф, счётчик болтов обнуляется.

→ Настройка `bolt3` (нет / −50 / −100).

## Бочка

На отметке **880** (или 850) счёт замораживается — нужно за один ход добрать **ровно до 1000** (то есть 120 при бочке 880). На бочке только один игрок; залез второй — первый слетает (со штрафом).

→ Настройка `barrelAt` (850 / 880 / без). Приложение показывает «−N до 1000» и подсвечивает игрока на бочке. Правило «второй сбивает первого» пока не списывает очки автоматически — только подсветка.

## Ямы

Две фиксированные ямы-ловушки: промежутки **200–300** и **600–700**. Внутри промежутка ты «в яме» на любом числе; между ними (300–600) и выше 700 — чисто. Важно: 555 лежит в свободной зоне между ямами — поэтому на самосвал реально попасть.

Выбраться — только перешагнув верх промежутка **за один ход**: добить счёт до 300 (или 700) или выше. Подползать мелкими конами нельзя. Пример: на 230 за ход нужно набрать минимум 70, чтобы выскочить на 300.

→ Настройка `pitLines`: каждая яма — независимый тумблер (включил/выключил), кнопка «нет» гасит обе. В приложении яма подсвечивается как напоминание (🕳 +70 до 300); очки она автоматически не режет — за выходом следят сами игроки.

## Обгон

Догнал соперника **точно** в его счёт — он откатывается назад.

→ Настройка `overtake` (нет / −50 / −100). Срабатывает только при точном равенстве счёта; обгоняющий остаётся на месте.

## Перебор

При правиле «ровно 1000» вылез выше — не засчитывается; на 950+ умнее остановиться. Иначе достаточно набрать 1000 или больше.

→ Настройка `exact` (да/нет).

## Тактика

Главное правило — «тише едешь, дальше будешь». Жадность до перебора и до болта — основная причина проигрышей.

---

### Сводка маппинга

| Правило игры | Настройка | Значения |
|---|---|---|
| До скольки играем | `target` | 1000 |
| Бочка | `barrelAt` | 850 · 880 · без |
| Шкала очков | `scale` | classic · fast |
| Ровно 1000 / перебор | `exact` | да · нет |
| Ямы | `pitLines` | тумблеры 200–300 / 600–700 (+ нет) |
| Штраф за болт | `samosval` | нет · −50 · −100 |
| Самосвал (555, по очкам) | `dump555` | выкл · 1 · 2 · 3 · всегда |
| 3 болта подряд | `bolt3` | нет · −50 · −100 |
| Обгон | `overtake` | нет · −50 · −100 |
| Порог входа | `entry` | без · 100 · 120 |
| Режим входа | `entryMode` | 1 заход · каждый раз |
